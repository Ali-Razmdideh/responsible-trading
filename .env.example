OPENAI_API_KEY=your_openai_key_here

# Optional: override the default model (default: gpt-5)
# PIPELINE_MODEL=gpt-5

# Optional: use mock LLM (no API calls, fully deterministic)
# PIPELINE_MOCK_LLM=true

# Optional: synthetic data seed (default: 42)
# PIPELINE_SEED=42
